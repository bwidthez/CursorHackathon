# Copy to .env and fill in real values. Never commit .env.

MINIMAX_API_KEY=

# Leave blank to use the built-in in-memory MongoDB (auto-seeded with demo data).
# Set a real URI to connect to Atlas or another external cluster.
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/rentshield

NEXT_PUBLIC_APP_URL=http://localhost:3000

# Python FastAPI backend URL (property reviews & vouchers service)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
